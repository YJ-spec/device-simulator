version: '3.8'

services:
  device-simulator:
    build: .
    container_name: device-simulator
    ports:
      - "5000:5000"
    environment:
      - MQTT_BROKER=mqtt
      - MQTT_PORT=1883
    restart: unless-stopped
