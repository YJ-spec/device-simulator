"""Deprecated legacy script.

This module is kept only for backward compatibility. Use `app.py` and
`device_manager.py` instead.
"""

if __name__ == "__main__":
    raise SystemExit(
        "device_simulator.py 已淘汰，請改用 app.py（多設備管理介面）。"
    )
